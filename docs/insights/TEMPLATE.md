# YYYY-MM-DD · <tag>

사람 머릿속을 거치는 유일한 고리라 가장 잘 샌다. **실험 직후** 적는다.
파일도 결과도 못 가져온다 — 화면을 보고 옮겨 적은 것만 남는다.

## RUN SUMMARY (그대로 옮겨 적기)

```
================ RUN SUMMARY ================
version   :
args      :
input     :
contract  :
metrics   :
runtime   :
status    :
=============================================
```

## 계약 위반 → `src/ragdiag/contracts.py`

한 줄씩 그대로. 어긋난 필드의 `note` 에 확인된 사실도 남긴다.

- [ ]

## 새로 터진 데이터 사고 유형 → `src/ragdiag/fixtures/synth.py`

그 유형을 합성으로 만들 수 있게 한다. 다음 사이클에서 여기서 재현된다.

- [ ]

## "코드 고치고 싶었던 순간" → `src/run.py`

그 값을 CLI 인자로 승격한다. **이 칸이 비어 있으면 대개 안 적은 것이다.**

- [ ]

## 의존성 충돌 → `requirements.txt`

`pip install --dry-run` / `pip check` 가 뱉은 메시지.

- [ ]

## 지표·규모·런타임 → 다음 실험 설계

- [ ]

## 판정 품질에 대한 인상

합성 데이터로는 알 수 없는 것. 오분류가 눈에 띈 case, 프롬프트가 헛도는 지점.
개별 대화 내용은 적지 않는다 — 무엇이 틀렸는지의 **유형**만.

- [ ]

---

반영이 끝나면 새 태그를 내고 다시 이식한다.
