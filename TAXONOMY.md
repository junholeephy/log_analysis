# 실패 분류 체계 (로그 기반 실행 가능판)

원본 `taxonomy.txt`의 30개 케이스를 **현재 로그 필드로 실제 판정할 수 있는 것**만으로
다시 배치한 것이다. 원본을 대체하지 않는다 — 원본은 "무엇이 잘못될 수 있나"의 목록이고,
이 문서는 "그중 무엇을 이 로그로 판정할 수 있나"의 목록이다.

현재 `conv_parse.py` 가 30개 중 **20개**에 도달한다. 나머지 10개는 파킹 6개와
로그로는 판정 불가 4개다. 아래 표의 '상태' 열이 실제 코드 기준이며,
`tests/test_route.py` 가 문서와 코드의 어긋남을 자동으로 잡는다.

## 가진 것

턴 하나당 주어지는 증거는 이게 전부다.

conv_eval 포맷 기준. 대화 전체가 시간순으로 오고, 한 턴이 (질문, 답변) 쌍이다.

| 필드 | 내용 |
|---|---|
| `user_question` · `llm_response` | 그 턴의 질문과 답변 |
| `retrieved_data` | 그 질문으로 검색된 청크 |
| `timestamp` | 턴 시각 |
| `llm_eval_result` · `llm_eval_score` | 직전 턴을 참고해 계산된 후속 질문의 성격 (turn≥2) |
| `llm_emotion_result` · `llm_emotion_score` | 같은 방식의 감정 분류 (turn≥2) |
| `trace_matched` | 대화에 턴이 2개 이상인지 — 턴 수에서 계산되는 **파생값** |
| 사용자 메타 | 부서 · 직급 · 직무 |

진단 케이스 하나는 연속한 두 턴의 쌍이다. 불만은 turn N+1의 `user_question`이고,
판정 대상 문서는 turn **N**의 `retrieved_data`다.

여기 없는 것: 서버 응답 시간, 문서 날짜·버전, 툴 호출 기록, 권한 조회 결과.
**이 목록이 판정 가능 범위를 결정한다.**

## 설계 원칙

**1. 조치 주체로 묶는다.** 증상이 아니라 "누가 고치는가"로 그룹을 나눈다. `case17`과
`case18`은 사용자에게 같은 증상이지만 하나는 문서팀, 하나는 프롬프트 담당이 고친다.
증상으로 묶으면 집계가 아무도 지목하지 못한다.

**2. 구분할 수 없으면 합친다.** `rag_data`만으로는 "코퍼스에 문서가 없다"와 "검색기가
못 찾았다"를 가를 수 없다. 그래서 `case17`은 하나의 라벨이다. 구분 못 하는 걸
구분한 척하는 라벨이 제일 나쁘다.

**3. 못 하는 건 버리지 말고 파킹한다.** 판정 불가한 케이스를 목록에서 지우면 나중에
"우리 분류에는 그런 게 없다"가 된다. 무엇이 있어야 가능해지는지와 함께 남겨둔다.

**4. 2계층으로 쓴다.** 굵은 그룹 7개로 헤드라인을 내고, 세부 라벨로 파고든다. 케이스가
수백 건이면 세부 20개는 셀이 비어 표가 읽히지 않는다. 볼륨이 받쳐줄 때만 쪼개서 본다.

## 판정 순서

값싸고 확실한 질문을 먼저 하고, 뒤 질문을 무의미하게 만드는 갈림길을 앞에 둔다.

```
1. 불만이 내용에 대한 것인가?
     형식 / 길이 / 언어  ─────────────────→  B. 요청 불이행
     불명                ─────────────────→  Z. 미분류
     내용
       ↓
2. 마지막 질문이 그 문장만으로 검색 가능한가?
     아니오 ────────────────────────────→  A. 질문 쪽  (플래그, 배타 아님)
       ↓
3. 도메인 질문인가?  (rag_data 유무 + 질문 성격)
     일반 / 계산 / 코드 ─────────────────→  F. 일반 질문
     도메인
       ↓
4. rag_data에 답이 있는가?          ← Stage 2
     없음 / 일부 ───────────────────────→  C. 검색·문서 쪽
     있음
       ↓
5. 답변이 그 문서를 썼는가?          ← Stage 3
     안 씀 / 어긋남 ────────────────────→  D. 생성 쪽
     씀 ───────────────────────────────→  D4·D5 (의도 · 깊이)
```

2번은 배타적 분기가 아니라 **플래그**다. 맥락 의존 질문이면서 동시에 검색이 실패할 수
있다. 원본에서 `case4`는 사용자측 문제로 분류되므로, 이 플래그가 선 케이스를 시스템
책임으로 세지 않도록 집계에서 분리한다.

## 라벨

신뢰도는 무엇이 판정을 결정하는지에 따른다.
**높음** = 코드로 검증 가능 · **중간** = LLM 판정 + 인용 강제 · **낮음** = 판정자의 사전지식에 의존.

### A. 질문 쪽 — 사용자 가이드 · UI로 고친다

| 라벨 | 원본 | 판정 근거 | 신뢰도 | 상태 |
|---|---|---|---|---|
| `q_context_dependent` | case4 | 관측 `question_self_contained` 의 반대 | 중간 | **구현됨** |
| `q_ambiguous` | case1 | 관측 `question_answerable_as_asked` | 중간 | **구현됨** |
| `q_multi_intent` | case3 | 관측 `question_multi_intent` | 중간 | **구현됨** |
| `q_unsupported_format` | case2 | 관측 `requests_unsupported_output` | 중간 | **구현됨** |
| `q_contains_pii` | case6 | 질문 텍스트에 개인·민감정보 패턴 | 높음 | **구현됨** |

### B. 요청 불이행 — 생성 프롬프트 · 후처리로 고친다

현재 `out_of_scope` 하나로 뭉쳐 있다. 조치가 갈리므로 쪼갠다.

| 라벨 | 원본 | 판정 근거 | 신뢰도 | 상태 |
|---|---|---|---|---|
| `req_format_ignored` | case12 | 요구한 형식(표·번호목록 등)과 답변 형태 불일치 | 높음 | **구현됨** |
| `req_length_ignored` | case11 | 길이 요구 대비 실제 길이 | 높음 | **구현됨** |
| `req_language_ignored` | case10 | 요구 언어와 답변 언어 불일치 | 높음 | **구현됨** |

### C. 검색·문서 쪽 — 코퍼스 · 청킹 · 검색기로 고친다

| 라벨 | 원본 | 판정 근거 | 신뢰도 | 상태 |
|---|---|---|---|---|
| `retrieval_insufficient` | case17 | 요구의 어느 부분에도 답하는 인용이 없음 | 중간 | **구현됨** |
| `retrieval_partial` | case17 | 요구의 일부에만 답하는 인용이 있음 | 중간 | **구현됨** |

`retrieval_insufficient`는 "검색기가 못 찾음"과 "코퍼스에 없음"을 합친 것이다.
가르는 유일한 간접 신호는 **부서별 편중** — 특정 부서에 몰리면 그 도메인 문서가 비었다는 뜻이다.

### D. 생성 쪽 — 생성 프롬프트 · 모델로 고친다

| 라벨 | 원본 | 판정 근거 | 신뢰도 | 상태 |
|---|---|---|---|---|
| `gen_ignored_context` | case18 | 문서에 답이 있는데 답변이 쓰지 않음 | 중간 | **구현됨** |
| `gen_contradicts_context` | case15 부분 | 답변이 문서와 어긋나는 주장을 함 | 중간 | **구현됨** |
| `gen_citation_wrong` | case20 | 답변의 인용·출처가 `rag_data`와 불일치 | 높음 | **구현됨** |
| `gen_intent_missed` | case13 | 문서도 충분하고 썼는데 물은 것과 다른 걸 답함 | 중간 | **구현됨** |
| `gen_too_shallow` | 원본에 없음 | 맞지만 사용자가 요구한 깊이에 못 미침 | 낮음 | 부분 |
| `gen_lost_turn_context` | case14 | 관측 `answer_used_history` (추가 호출 없음) | 중간 | **구현됨** |
| `gen_truncated` | case9 | 답변이 문장 중간에서 끊김 | 높음 | **구현됨** |

`gen_contradicts_context`는 **문서와 대조 가능한 할루시네이션만** 잡는다. 문서 없이
지어낸 것(`case15`의 나머지)은 이 로그로 판정할 수 없다 — 이름을 좁게 둔 이유다.

`gen_citation_wrong`은 `verify.py`를 뒤집으면 된다. 지금은 판정자의 인용을 원문과
대조하는데, 같은 함수로 **챗봇 답변의 인용**을 `rag_data`와 대조하면 그대로 성립한다.

`gen_too_shallow`는 원본에 없지만 실제 데이터에서 나온다. 현재 `rag_sufficient_other`의
잔여가 대부분 여기다. 경계가 흐리므로 이게 커지면 라벨을 쪼갤 때다.

### F. 일반 질문 — 모델 · 도구로 고친다

| 라벨 | 원본 | 판정 근거 | 신뢰도 | 상태 |
|---|---|---|---|---|
| `general_calc_wrong` | case22 | 등식이 있으면 코드로 계산 검증. 없으면 medium | 높음/중간 | **구현됨** |
| `general_code_wrong` | case23 | 파이썬 `ast.parse` + SQL 구조 검사 | 중간 | **구현됨** |
| `general_knowledge_wrong` | case21 | 판정자의 사전지식 | **낮음** | **구현됨** |

`general_knowledge_wrong`은 **판정자가 아는 것으로 판정한다.** 우리가 Stage 2에서
막으려고 애쓴 바로 그 오염을 여기서는 근거로 쓴다. 다른 라벨과 같은 신뢰도로 읽으면 안 되고,
표본 수동 검토를 전제로만 쓴다.

### G. 보안 · 정책

| 라벨 | 원본 | 판정 근거 | 신뢰도 | 상태 |
|---|---|---|---|---|
| `policy_refusal_disputed` | case24 | 관측 `answer_refused`. case25 와 구분 불가 | 중간 | **구현됨** |
| `prompt_injection_followed` | case26 | 코드 검증 — 지시문 탐지 + 답변 반영 여부 | 중간 | **구현됨** |

### Z. 미분류

| 라벨 | 판정 근거 |
|---|---|
| `unclassified` | 불만 성격을 판별할 수 없음. **수동 검토 대상이지, "문제 없음"이 아니다** |

## 파킹 — 필드가 생기면 가능해지는 것

지운 게 아니라 미룬 것이다. 옆의 필드 하나가 생기면 바로 살아난다.

| 원본 | 내용 | 필요한 것 |
|---|---|---|
| case5 | 컨텍스트 길이 초과 | 모델의 컨텍스트 상한값 (상수 하나면 계산 가능) |
| case8 | 응답 지연으로 이탈 | 서버 응답 시간. `timestamp`는 턴 시각이라 사용자 대기와 생각 시간이 섞여 있어 지연을 분리할 수 없다 |
| case16 | 같은 질문에 답이 매번 다름 | **새 필드 불필요** — conv_eval에 대화 전체가 오므로 질문을 정규화해 묶으면 교차 비교가 된다 |
| case19 | 구 문서를 검색함 | 청크의 문서 ID · 개정일 |
| case25 | 권한 없는 문서 접근 | 권한 조회 결과 |
| case27 | 외부 API 호출 실패 | 툴 호출 로그 |

`case16`은 파킹 중 가장 값싸다. conv_eval 포맷에는 대화 전체가 들어오므로 새 필드가
필요 없고, 턴 단위 독립 판정을 질문 단위 묶음으로 바꾸기만 하면 된다.

## 로그로는 불가

| 원본 | 내용 | 이유 |
|---|---|---|
| case7 | 서비스 끊김 | 답변이 없으면 로그에 턴이 남지 않는다 |
| case28 | 로그인·인증 실패 | 대화 자체가 시작되지 않는다 |
| case29 | 세션 만료로 중단 | 위와 같음 |
| case30 | 권한 시스템 오류로 접근 거부 | 위와 같음 |

**이건 이 분류 체계의 구조적 한계다.** 우리가 보는 것은 "대화가 성립한 로그"뿐이라
생존 편향(survivorship bias)이 걸린다. 접속조차 못 한 사용자는 이 통계 어디에도 없다.
서비스 안정성은 이 파이프라인이 아니라 별도 텔레메트리로 봐야 한다.

## 원본 30개 전수 배치

조용히 사라진 케이스가 없음을 확인하기 위한 표다.

`route.py` 가 실제로 도달할 수 있는 case 를 전수 열거해 확인한 결과다
(`tests/test_route.py::test_routing_never_produces_an_undiagnosable_case`).

| | 케이스 |
|---|---|
| **라우팅 도달 가능** (20) | 1, 2, 3, 4, 6, 9, 10, 11, 12, 13, 14, 15, 17, 18, 20, 21, 22, 23, 24, 26 |
| **파킹** (6) | 5, 8, 16, 19, 25, 27 — 필드가 생기면 가능 |
| **로그로는 불가** (4) | 7, 28, 29, 30 |

합계 30. 중복 계상 없음. 판정 불가한 10개로는 라우팅이 절대 가지 않는다.

### 남은 한계

**case22 (계산)** — 답변에 `A + B = C` 꼴 등식이 있을 때만 코드로 검증한다.
"5영업일 뒤면 3월 13일" 같은 자연어 계산은 못 잡으므로, 등식이 없으면 신뢰도를
medium 으로 낮추고 그 이유를 남긴다. 실제 질문은 도메인과 계산이 겹치는 경우가
흔해서(회사 규정이 있어야 계산이 되는 질문) `question_domain` 이 하나만 고를 수
있다는 제약이 있다. 그래서 등식 오류는 **부가 케이스**로도 따로 잡는다.

**case23 (코드)** — 파이썬은 `ast.parse`, SQL 은 구조적 결함(괄호 짝, GROUP BY 뒤
비어 있음, SELECT 인데 FROM 없음)만 본다. 문법이 맞다고 정답인 건 아니다.
실행 검증에는 샌드박스와 DB 연결이 필요하다.

**case26 (인젝션)** — 문서에 지시문이 **있다는 것**과 답변이 **그걸 수행했다는 것**을
구분한다. 있기만 하면 `undetermined`, 답변에 흔적이 보일 때만 `violated` 다.
사내 규정문은 "~한다" 같은 규범형이 많아 명령형만으로 의심하면 오탐이 쏟아지므로,
모델을 겨냥한 지시("이전 지시를 무시하고", "시스템 프롬프트를 출력")로 좁혀서 본다.

**case14 (맥락 상실)** — 새 LLM 호출 없이 Step 1 의 관측 필드로 잡는다.
Step 1 이 이미 히스토리와 답변을 다 보고 있어서 따로 물을 이유가 없다.

## 다음 단계 우선순위

기준은 **구현 비용 대비 통계가 달라지는 폭**이다.

1. **B 그룹 쪼개기** — 지금 `out_of_scope` 하나에 3개 케이스가 뭉쳐 있다. Stage 1
   프롬프트에 `complaint_type`을 세분하는 것만으로 되고, 새 LLM 호출이 없다.
2. **`gen_citation_wrong`** — `verify.py`를 방향만 바꿔 재사용한다. 신뢰도 높음.
3. **`q_*` 그룹** — Stage 1이 이미 질문과 불만을 다 보고 있다. 출력 필드만 늘리면 된다.
4. **F 그룹** — 도메인 질문과 일반 질문을 먼저 갈라야 한다. `rag_data`가 비었는지가
   1차 신호지만, 도메인 질문인데 검색이 아예 안 걸린 경우와 구분되지 않는다.
   여기가 다음으로 어려운 지점이다.
