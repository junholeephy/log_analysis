# 실패 분류 체계 개정안

원본 `taxonomy.txt` 를 구현하면서 실제로 부딪힌 것만 반영한 개정안이다.
추측으로 다듬은 부분은 없다 — 각 변경에는 무엇이 걸렸는지 근거를 달았다.

**기존 case 번호는 건드리지 않았다.** 재번호를 매기면 이미 붙인 라벨, 필터 로직,
구현 코드가 전부 어긋난다. 새 케이스는 31번부터 이어 붙였다.

## 무엇이 바뀌었나

| # | 변경 | 근거 |
|---|---|---|
| 1 | **케이스마다 관측 가능성 표시** | 관측 불가 케이스가 통계에서 0으로 나오는데, 표시가 없으면 "그 문제는 없네"로 읽힌다 |
| 2 | **구분 불가한 쌍 명시** (case17 내부, case24/25) | 고칠 팀이 다른데 로그로는 못 가른다. 구분한 척하는 게 제일 나쁘다 |
| 3 | **case3 을 둘로 분리** → case3 + case31 | 질문이 복합인 것과 답변이 일부만 다룬 것은 고칠 곳이 다르다 |
| 4 | **case32 말투·어조 추가** | 회귀셋에서 실제로 갈 곳이 없어 미분류로 떨어졌다 |
| 5 | **case33 얕은 답변 추가** | 문서도 충분하고 활용도 했는데 불만인 경우가 실제로 나온다 |
| 6 | 각 케이스에 **판정 근거** 명시 | 무엇으로 판정하는지가 없으면 구현할 수 없다 |

관측 표기: **가능** = 현재 로그로 판정 가능 · **필드** = 필드 하나 추가하면 가능 ·
**불가** = 로그로는 원리적으로 판정 불가

---

## category_1 · 사용자측 문제

### TYPE1 — 적절하지 않은 질문/요청
> 모델이 사용자로 하여금 정확한 질문을 하도록 유도해야 하는 경우

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **1** | 이해하기 어려운 질문으로 답변을 요구 | 가능 | 질문만으로 답을 특정할 수 없음 |
| **2** | 지원하지 않는 포맷의 답변을 요구 (외부 링크, 그림 생성, 파일 첨부) | 가능 | 요구한 출력 형태가 텍스트로 낼 수 없는 것 |
| **3** | 한 번에 여러 의도를 섞은 복합 질문을 함 | 가능 | 질문에 서로 다른 요구가 둘 이상 |
| **4** | 이전 대화를 전제로 참조가 모호한 질문 (예: 그거 다시 알려줘) | 가능 | 질문 문장에 대상 명사가 없거나 지시대명사 존재 |
| **5** | 컨텍스트 길이를 초과하는 입력으로 답변 품질 저하 | **필드** | 모델의 컨텍스트 상한값(상수 1개)이 있으면 계산 가능 |
| **6** | 질문에 실수로 개인정보/민감정보가 포함됨 | 가능 | 질문 텍스트의 패턴 검출 |

> **case3 변경.** 원본은 "복합 질문으로 **일부만 답변되는** 경우"였다. 두 사건이
> 한 문장에 있었다 — 사용자가 복합 질문을 한 것(질문 특성)과 모델이 일부만 답한 것
> (응답 실패). 고칠 곳이 다르므로 후자를 **case31** 로 분리했다.
> case3 은 "질문을 쪼개도록 유도해야 한다", case31 은 "모든 부분에 답해야 한다".

---

## category_2 · LLM 서비스측 문제

### TYPE2 — 서비스 안정성 문제

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **7** | 서비스 끊김으로 답변을 못 함 | **불가** | 답변이 없으면 로그에 턴 자체가 남지 않는다 |
| **8** | 응답은 오나 지연이 심해 사용자가 이탈 | **필드** | 서버 응답 시간. `timestamp`는 사용자 생각 시간이 섞여 있어 대체 불가 |
| **9** | 답변이 토큰 제한 등으로 중간에 잘림 | 가능 | 종결 부호 부재 · 코드블록 미닫힘 |

> **이 타입은 통계가 왜곡된다.** case7 은 로그에 남지 않으므로 분포에서 항상 0 이다.
> "서비스 안정성 문제는 없었다"로 읽으면 안 된다 — **측정하지 않은 것이지 없는 것이 아니다.**
> 서비스 안정성은 이 분류 체계가 아니라 별도 텔레메트리로 봐야 한다.

### TYPE3 — 사용자의 의도를 파악하지 못함
> 언어/길이/포맷/기타 요청 불이행

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **10** | 특정 언어 요구를 만족하지 못함 | 가능 | 요구 언어와 답변 언어 불일치 (문자 종류 판별) |
| **11** | 길이 요구를 만족하지 못함 | 가능 | 요구 대비 실제 글자·문장·줄 수 |
| **12** | 특정 포맷 요구를 만족하지 못함 | 가능 | 요구한 구조(표·번호목록 등)의 부재 |
| **13** | 의도를 파악하지 못해 기대와 다른 답변 | 가능 | 문서도 충분하고 활용도 했는데 요구와 다른 것을 답함 |
| **14** | 멀티턴에서 이전 턴 맥락을 잊거나 잘못 연결 | 가능 | 이전 턴에서 정해진 조건을 답변이 어김 |
| **31** | **복합 질문의 일부만 답변함** ⟵ 신규 | 가능 | 질문의 요구 중 일부가 답변에 없음 |
| **32** | **말투·어조·용어 요구를 지키지 않음** ⟵ 신규 | 가능 | 요구한 어조와 실제 어조 불일치 |
| **33** | **내용은 맞으나 실행할 수 있는 수준이 아님** ⟵ 신규 | 가능 | 문서 충분·활용 확인됐으나 사용자가 다음 행동을 알 수 없음 |

> **case14 와 case4 의 경계.** 둘 다 "맥락"을 다루지만 주체가 다르다.
> case4 는 **질문**이 불완전한 것, case14 는 **답변**이 히스토리를 못 쓴 것이다.
> 구현에서 이 둘이 섞여 오분류가 났다 — 검색이 실패해 답변이 부실하면 모델이
> 그걸 "히스토리를 못 이어받았다"로도 읽기 때문이다. **증거가 약한 쪽(case14)을
> 강한 쪽(case17/18)보다 먼저 판정하면 안 된다.**
>
> **case32 신규.** "말투가 딱딱하고 영어 용어가 많다"는 불만이 실제 회귀셋에 있었는데
> 갈 곳이 없어 미분류로 떨어졌다. case10~12 는 언어·길이·포맷이고 어조는 별개다.
>
> **case33 신규.** "내용은 맞는데 그래서 뭘 해야 하는지 모르겠다" 유형.
> case13(다른 것을 답함)과 다르다 — 물은 것은 맞게 답했으나 행동으로 이어지지 않는다.
> 고칠 곳도 다르다: case13 은 의도 이해, case33 은 답변의 구체성.

### TYPE4 — 할루시네이션 답변

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **15** | 사실과 다른 정보를 제공 | **부분** | 검색 문서와 **대조 가능한 것만** 판정. 문서 밖 허구는 불가 |
| **16** | 동일/유사 질문에 답이 매번 다름 (응답 일관성) | **필드** | 새 필드는 불필요하나 **턴 단위가 아니라 로그 전체**를 훑어야 함 |

> **case15 의 범위를 좁혀야 한다.** 지금 문장은 모든 할루시네이션을 포함하는데,
> 실제로 판정 가능한 것은 "답변이 검색 문서와 어긋나는 주장을 한 경우"뿐이다.
> 문서 없이 지어낸 것은 판정자의 사전지식에 의존하게 되고, 그건 신뢰할 수 없다.
> 원본 그대로 두면 "할루시네이션 3%"가 실제보다 훨씬 낮게 나오면서 정확해 보인다.

### TYPE5 — 도메인 관련 Retrieve Context 문제
> 검색/활용 실패

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **17** | Context Retrieve 를 적절히 하지 못함 | 가능 | 검색된 문서에 요구된 정보가 없음 (인용 검증) |
| **18** | Retrieve 는 잘했으나 모델이 적절히 답하지 못함 | 가능 | 문서에 답이 있는데 답변이 쓰지 않음 |
| **19** | 최신 문서 대신 구 문서를 retrieve (최신성) | **필드** | 청크의 문서 ID·개정일 |
| **20** | 내용은 맞으나 출처/인용 표기가 틀리거나 누락 | **부분** | 답변의 **인용문**만 대조 가능. 문서명·조항번호는 메타데이터 없이 불가 |

> **case17 은 두 원인을 합친 것이다.**
> (a) 코퍼스에 문서가 있는데 검색기가 못 찾음 → **검색기·임베딩·청킹**을 고쳐야 한다
> (b) 코퍼스에 문서가 애초에 없음 → **문서를 써야 한다**
>
> 가져온 top-k 에 답이 없다는 사실은 코퍼스 어딘가에 답이 있는지에 대해 아무것도
> 말해주지 않는다. **코퍼스 전체에 접근하지 않는 한 이 둘은 구분할 수 없다.**
> 간접 신호는 **부서별 편중** 하나뿐이다 — 특정 부서에 몰리면 (b)일 가능성이 높다.
>
> 이걸 두 케이스로 쪼개지 않은 이유: 배정할 수 없는 라벨은 죽은 라벨이다.
> 대신 case17 아래에 두 원인을 명시하고, 분리 조건(코퍼스 접근)을 함께 적는다.

### TYPE6 — 일반성 질문
> 상식/계산 질문 및 기술·도구(SQL, Python, Spotfire, Excel 등) 사용법

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **21** | 상식 질문에 적절히 답하지 못함 | **낮은 신뢰도** | 판정자의 사전지식 — 다른 케이스와 같은 무게로 집계하면 안 됨 |
| **22** | 수식·날짜·산수 계산이 틀림 | **부분** | 답변에 등식이 있으면 코드로 재계산. 자연어 계산은 불가 |
| **23** | 코드·도구 사용법이 부정확하거나 실행 불가 | **부분** | 문법 검사까지. 실행 검증에는 샌드박스 필요 |

> **case21 은 다른 케이스와 성격이 다르다.** 판정자가 아는 것으로 판정하므로,
> 이 도구가 문서 판정에서 그토록 막으려 한 **사전지식 오염을 여기서는 근거로 쓴다.**
> 집계할 때 반드시 신뢰도로 구분해야 하고, 표본 수동 검토를 전제로만 써야 한다.

### TYPE7 — 보안/정책 제한

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **24** | 보안 정책상 답변할 수 없는 정보를 요청 | 가능 | 답변이 정책·보안을 명시적 이유로 거절 |
| **25** | 권한이 없는 문서/데이터에 대한 접근을 요청 | **불가** | 권한 조회 결과 필요. **case24 와 로그상 동일하게 보임** |
| **26** | Retrieve 된 문서 내 숨겨진 지시를 모델이 수행 (간접 프롬프트 인젝션) | 가능 | 문서의 지시문 패턴 + 답변에의 반영 여부 |
| **27** | 외부 API/DB 호출 시 잘못된 파라미터 또는 실패 | **불가** | 툴 호출 로그 필요 |

> **case24 와 case25 는 로그상 구분되지 않는다.** 둘 다 "답변이 거절했다"로만
> 보인다. 권한 조회 결과가 있어야 갈리므로, 그때까지는 case24 로 모으고
> 그 안에 case25 가 섞여 있음을 명시한다.
>
> **case26 은 두 사건을 구분해야 한다.** 문서에 지시문이 **있는 것**과 모델이
> 그걸 **수행한 것**은 다르다. 전자만으로 위반이라 하면, 사내 규정문의 규범형 서술
> ("~한다")이 대량 오탐된다.

---

## category_3 · 인프라/권한 시스템측 문제

### TYPE8 — 인프라·권한 시스템 오류

| case | 내용 | 관측 | 판정 근거 |
|---|---|---|---|
| **28** | 로그인/인증 실패로 서비스 접근 불가 | **불가** | 대화가 시작되지 않아 로그가 없음 |
| **29** | 세션 만료로 대화 중 답변이 중단됨 | **불가** | 위와 같음 |
| **30** | 권한 조회 오류로 정상 권한임에도 접근 거부 | **불가** | 위와 같음 |

> **이 카테고리 전체가 대화 로그로는 관측되지 않는다.** 우리가 보는 것은
> "대화가 성립한 로그"뿐이므로 생존 편향(survivorship bias)이 걸린다.
> 접속조차 못 한 사용자는 이 통계 어디에도 없다.
>
> 원본에서 이 카테고리만 `type_name` 이 없었는데, 이름을 붙였다.

---

## 관측 가능성 요약

| | 케이스 | 수 |
|---|---|---|
| 현재 로그로 판정 가능 | 1, 2, 3, 4, 6, 9, 10, 11, 12, 13, 14, 17, 18, 24, 26, 31, 32, 33 | 18 |
| 부분적으로만 가능 | 15(문서 대조분만), 20(인용문만), 22(등식만), 23(문법만) | 4 |
| 신뢰도 낮음 | 21(판정자 사전지식) | 1 |
| 필드 추가하면 가능 | 5, 8, 16, 19 | 4 |
| 로그로는 불가 | 7, 25, 27, 28, 29, 30 | 6 |

합계 33 (기존 30 + 신규 3).

**"판정 가능" 18개가 이 도구가 실제로 다루는 범위다.** 나머지 15개는 목록에
남겨두되, 집계 결과를 읽을 때 "0건"이 "문제 없음"이 아니라는 것을 알아야 한다.

---

## 판단이 필요한 곳

제가 정하지 않고 남겨둔 것들이다.

**① case3 분리에 동의하시는지.** 원본의 한 문장을 둘로 나눴다. 기존에 case3 으로
라벨링된 데이터가 있다면 재분류가 필요하다.

**② case33(얕은 답변)이 실제로 필요한지.** 실데이터 분포를 보기 전에는 확신할 수
없다. 지금은 case13 에 섞여 들어가는데, 그 비중이 크지 않으면 굳이 나눌 필요가 없다.

**③ case15 의 문장을 좁힐지.** "사실과 다른 정보"를 "검색 문서와 어긋나는 주장"으로
좁히면 정직해지지만, 문서 없는 할루시네이션을 담을 곳이 사라진다. 별도 케이스를
만들고 "관측 불가"로 두는 방법도 있다.

**④ case25 를 case24 에 흡수할지, 따로 둘지.** 로그로는 구분되지 않지만 개념적으로는
다른 문제다. 권한 시스템 연동 계획이 있다면 따로 두는 게 맞다.
